{
  "name": "invitation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run build && node script/deploy.js",
    "build" : "rm -rf dist && cp -r src dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
